# AFTON editorial core: current agent instructions

## Active scope
The user is preparing the visual design. Agents may proceed with non-design content architecture, code, tests and Git records. Do not create further design mockups, images, palettes, fonts or final layout choices. The rejected generated mockups are not implementation references.

Build on the existing Astro repository; do not restart the project or introduce a CMS/database. Current slice: Home, Issue, Article, Scent Dossier and Info. Material, Object, Record, Product and Availability are data entities, not new route requirements.

## Source and privacy boundary
- Website code and independently authorized AFTON content only.
- Personal/background documents are collaboration context, not permission to publish, paraphrase, or import them into pages, fixtures, metadata, prompts or build output.
- No formulas, private research, customer data, account details or confidential partner material.
- Keep the 14 external references and their history intact. They are research-only, never production assets.
- Preserve the official wordmark. No logo approximation.
- Do not merge unrelated archive research from main into this implementation slice.

## Current work
Read CODEX_TASK.md and docs/EDITORIAL_CORE_V01.md. The corrected context-only boundary in docs/20_DIRECTION_APPROVAL_AND_FOUNDER_HANDOFF.md remains in force. Historical Phase B stop language does not prevent the delegated non-design core work; it does not authorize generation or release either.

Keep content contracts and page view models independent of the final visual design. Candidate fixtures must remain labeled and must not be promoted automatically. Unknown facts stay unknown. No inferred prices, launch dates, claims or contacts.

## Checks
Use the committed package lock and Node >=22.12.0. Run npm run verify:editorial. The optional browser smoke harness needs Playwright in the test environment, not the site bundle. Capture actual output and tested SHA. A previous PR result is not a current test result.

## Release boundary
GENERATION HOLD / CREDIT SPEND HOLD / MERGE HOLD / DEPLOY HOLD.
No checkout, real inventory sync, personalization, FMOC2 integration, login, analytics, waitlist or public deployment. npm run deploy deliberately fails. Review/noindex labels are not access control.

Do not force-push or overwrite another agent's changes. Re-read refs before writing. Use isolated candidate work; do not merge PR #1 into main. Filename normalization is a separate technical task and does not block core work. Future generation, only if separately authorized, follows the existing Codex/Higgsfield-browser rule.

Report a concise Git receipt: changes, actual checks, blockers, next owner. Never claim a timer or Codex run exists merely because a handoff was written.
